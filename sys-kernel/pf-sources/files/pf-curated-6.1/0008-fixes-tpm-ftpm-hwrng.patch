From: Oleksandr Natalenko <oleksandr@natalenko.name>
Subject: [pf-curated-6.1] fixes: tpm: disable hwrng for fTPM on some AMD designs

Re-cut from natalenko's pf-kernel branch fixes-6.1 (tip 878951ae5a6)
as a single bulk diff, restricted to the files this overlay's curation
kept on the 6.1 LTS slot. Source: codeberg.org/pf-kernel/linux.

Single-file backport of the upstream tpm-fixes commit disabling fTPM
hwrng exposure on AMD designs where it produces stalls. Touches only
drivers/char/tpm/tpm-chip.c.
---
diff --git a/kernel/user_namespace.c b/kernel/user_namespace.c
index 54211dbd516..16ca0c15162 100644
--- a/kernel/user_namespace.c
+++ b/kernel/user_namespace.c
@@ -22,6 +22,13 @@
 #include <linux/bsearch.h>
 #include <linux/sort.h>
 
+/* sysctl */
+#ifdef CONFIG_USER_NS_UNPRIVILEGED
+int unprivileged_userns_clone = 1;
+#else
+int unprivileged_userns_clone;
+#endif
+
 static struct kmem_cache *user_ns_cachep __read_mostly;
 static DEFINE_MUTEX(userns_state_mutex);
 
